// Trail Log — optional exported World label overrides.
// Use World Map → Name labels → Tune Labels → Export Overrides to generate
// a replacement for this file. Keep it as a classic script for file:// use.

const WORLD_LABEL_MANUAL_OVERRIDES = {
  anchors: {},
  dots: {},
  synthetic: {}
};
